# Feishu 2026.09 RC1 validation

Date: 2026-09-14. Status: **candidate, not promoted to production**.

## Implemented and verified locally

- Three product forks start from official releases: Headscale v0.29.3, Headplane v0.7.1, Casdoor v4.3.0. Exact downstream commits are in `../versions.lock.yaml`.
- All downstream commits use JUN WEI WANG <wjw_03@outlook.com>. Product `main`/`master` refs are unchanged. Feature work was isolated in worktrees and integrated without merge commits.
- Casdoor focused Lark/OAuth tests and backend source build passed using Go 1.25.8.
- Headscale focused OIDC/configuration tests, concurrent callback race tests, and backend source build passed using Go 1.26.5.
- Integration Python suite: 39 tests passed (27 worker/identity, 9 release/restore safety, 3 runtime rendering).
- Docker Compose v5.5.1 parsed the rendered configuration with every profile enabled. This validates configuration syntax, not service startup.
- Built Headscale accepted generated configuration against a local mock OIDC issuer and initialized isolated temporary state.
- Shell syntax checks, source-lock verification, and Git whitespace checks passed.
- Restore tests use a fake Docker command to exercise archive safety and path relocation. They are not evidence of PostgreSQL restore or full disaster recovery.

## Deployment inputs still required

- Public hostnames, DNS/TLS, and target deployment host with Docker Engine/Compose.
- Published Feishu enterprise app, App ID, secret files, approved Contact permissions/data scope, explicit admission department/group IDs, and selected admin-role mapping.
- Casdoor initialization, signing certificate, confidential OIDC clients, native syncer configured with immutable Lark key and disjoint ownership, and dedicated worker API credentials.
- An intended Headplane first owner and an approved initial ACL policy.

## Remaining acceptance evidence

- Real Feishu OAuth and native import in both identity-creation orders; no duplicate identities.
- Signed ID token/UserInfo claims accepted by both downstream OIDC clients; correct tenant scope and group/role decisions.
- Headplane frontend/container build and all service startup checks on the deployment host.
- Existing VPN node/preauth-key revocation and Headplane session expiry during offboarding. The worker blocks future broker authentication; operators perform existing-device revocation according to the runbook.
- Fresh-host backup/restore and sequential upgrade/rollback drill.
- Employee pilot using exact supported client versions and one allowed/one denied resource.
- Image digests, build toolchain/base-image provenance, and a completed release manifest.

The production promotion checker deliberately rejects the current incomplete manifest. RC branches are reviewable delivery artifacts; branch creation does not certify a production deployment. No credentials or production data were supplied or used in the local fixture tests.
